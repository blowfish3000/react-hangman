import { languagesData } from "./languages";
import React from "react";

export default  function Language(props){
    <p backgroundColor={props.backgroundColor} color={props.color}>{props.name}</p>
}